import React from "react";
// import Button from "@material-ui/core/Button";
import { Alert } from "react-bootstrap";

const Navbar = () => {
  return (
    <>
      <Alert variant="primary">
        <h1 className="title">MODDLE KEEP</h1>
      </Alert>
    </>
  );
};
export default Navbar;
